# Discarded Or Deferred Candidates

| candidate | status | reason | revisit condition |
| --- | --- | --- | --- |
| Pensieve | deferred, not discarded scientifically | Neural/RL controller requires training, model handling, reward definition, and trace/simulator methodology outside this block. | Revisit only in a future AI/RL phase. |
| dash.js DYNAMIC | deferred | Hybrid production behavior would blur initial academic baseline comparison. | Revisit after BOLA and rate-based baselines are implemented and evaluated. |
| dash.js FAST SWITCHING | deferred | Segment replacement behavior may modify runtime semantics and is not needed for the initial baseline set. | Revisit only as a practical player-extension topic. |
| SODA | deferred optional | Strong modern candidate but outside the initial implementation order. | Revisit after mandatory baselines and QoE definition. |
| RBC | backup optional | Not selected and source identity must be locked before use. | Revisit only if a documented comparison gap remains. |

No discarded/deferred candidate may create runtime requirements in this block.

## Phase 2 Closure Status

Phase 2 closes with Pensieve as historical IA/RL reference only, DYNAMIC and FAST SWITCHING deferred as practical dash.js-derived methods, SODA optional/deferred and RBC backup optional. None of these methods is implemented or benchmarked in Phase 2.
