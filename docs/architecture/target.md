El cliente debe ser:

ABR-neutral
no meter heurísticas escondidas que favorezcan o penalicen controllers.
plugable
cualquier controller entra por la misma interfaz.
reproducible
misma config, mismo comportamiento.
observable
métricas homogéneas y comparables.
dual-mode
fake engine para pruebas controladas y GStreamer para validación real.